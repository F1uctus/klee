# -*- Python -*-

# These tests link a natively compiled program against libkleeRuntest to replay
# a generated test case, so they need that library to have been built.
if 'libkleeruntest' not in config.available_features:
    config.unsupported = True
