# -*- Python -*-

# ConcreteTest.py builds each test's bitcode by invoking GNU make on
# Makefile.cmake.test, so the whole directory needs make on PATH. A stock
# Windows toolchain has no make, and reporting these as failures there hides
# real regressions in the noise.
if 'make' not in config.available_features:
    config.unsupported = True
