# -*- Python -*-

# These tests replay a generated test case against a natively compiled program
# through klee-replay, which is POSIX-only and not built everywhere.
if 'klee-replay' not in config.available_features:
    config.unsupported = True
